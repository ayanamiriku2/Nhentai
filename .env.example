# Environment variables for the mirror proxy
# Copy this file to .env and fill in the values

# Your mirror domain (without protocol)
# e.g. yourdomain.com or mirror.yourdomain.com
MIRROR_DOMAIN=

# Target website to mirror
TARGET_HOST=nhentai.xxx

# Port (Railway/Render auto-set this via PORT env var)
PORT=3000
